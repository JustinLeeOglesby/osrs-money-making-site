# Python deps for the Flask web app (deployed to Render).
# Kept separate from the bot project's requirements.txt so a deploy
# doesn't pull in opencv / pyautogui / pywin32.
flask>=3.0.0
flask-cors>=4.0.0
requests>=2.31.0
openpyxl>=3.1.0
gunicorn>=21.0.0
# Cross-device sync (Postgres-backed key/value store keyed by passphrase hash).
# psycopg2-binary ships a precompiled wheel so Render's build doesn't need a C compiler.
psycopg2-binary>=2.9.0
# Inventory-screenshot OCR via Claude vision. Optional — endpoints 503
# gracefully when ANTHROPIC_API_KEY isn't set, so the rest of the app
# works fine without it.
anthropic>=0.40.0

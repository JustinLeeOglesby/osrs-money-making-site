# Python deps for the Flask web app (deployed to Render).
# Kept separate from the bot project's requirements.txt so a deploy
# doesn't pull in opencv / pyautogui / pywin32.
flask>=3.0.0
flask-cors>=4.0.0
requests>=2.31.0
openpyxl>=3.1.0
gunicorn>=21.0.0
